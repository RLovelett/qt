# handle pkg-config files
QMAKE_CXXFLAGS += $$system(pkg-config --cflags '$$quote(PKGCONFIG)')
QMAKE_CFLAGS += $$system(pkg-config --cflags '$$quote(PKGCONFIG)')
LIBS += $$system(pkg-config --libs '$$quote(PKGCONFIG)')
